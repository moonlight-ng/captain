diff --git a/dist/index.cjs b/dist/index.cjs
index 4709730f1d6608dee4ffb3a386d1732f776c5f5c..991c8f0ae940ba12b845d3fa9707d5d4f7591a94 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -83,9 +83,9 @@ function resolveTheme(color, mode) {
     // Background
     bgRgb: isDark ? [10, 10, 10] : [255, 255, 255],
     // Fonts
-    labelFont: '11px "SF Mono", Menlo, Monaco, "Cascadia Code", monospace',
-    valueFont: '600 11px "SF Mono", Menlo, monospace',
-    badgeFont: '500 11px "SF Mono", Menlo, monospace'
+    labelFont: '11px "Libre Franklin", ui-sans-serif, sans-serif',
+    valueFont: '600 11px "Libre Franklin", ui-sans-serif, sans-serif',
+    badgeFont: '500 11px "Libre Franklin", ui-sans-serif, sans-serif'
   };
 }
 var SERIES_COLORS = [
@@ -639,7 +639,7 @@ function drawCrosshair(ctx, layout, palette, hoverX, hoverValue, hoverTime, form
   const separator = "  \xB7  ";
   ctx.save();
   ctx.globalAlpha = scrubOpacity;
-  ctx.font = '400 13px "SF Mono", Menlo, monospace';
+  ctx.font = '400 13px "Libre Franklin", ui-sans-serif, sans-serif';
   const valueW = ctx.measureText(valueText).width;
   const sepW = ctx.measureText(separator).width;
   const timeW = ctx.measureText(timeText).width;
@@ -691,7 +691,7 @@ function drawMultiCrosshair(ctx, layout, palette, hoverX, hoverTime, entries, fo
   if (scrubOpacity < 0.1 || layout.w < 300) return;
   ctx.save();
   ctx.globalAlpha = scrubOpacity;
-  ctx.font = '400 13px "SF Mono", Menlo, monospace';
+  ctx.font = '400 13px "Libre Franklin", ui-sans-serif, sans-serif';
   ctx.textAlign = "left";
   const timeText = formatTime(hoverTime);
   const sep = "  \xB7  ";
@@ -757,7 +757,7 @@ function drawReferenceLine(ctx, layout, palette, ref) {
   if (y < pad.top - 10 || y > h - pad.bottom + 10) return;
   const label = ref.label ?? "";
   if (label) {
-    ctx.font = "500 11px system-ui, sans-serif";
+    ctx.font = '500 11px "Libre Franklin", ui-sans-serif, sans-serif';
     const textW = ctx.measureText(label).width;
     const centerX = pad.left + chartW / 2;
     const gapPad = 8;
@@ -1018,7 +1018,7 @@ function drawOrderbook(ctx, layout, palette, orderbook, dt, state, swingMagnitud
   state.labels.length = writeIdx;
   const baseAlpha = ctx.globalAlpha;
   ctx.save();
-  ctx.font = '600 13px "SF Mono", Menlo, monospace';
+  ctx.font = '600 13px "Libre Franklin", ui-sans-serif, sans-serif';
   ctx.textAlign = "left";
   ctx.textBaseline = "middle";
   ctx.globalAlpha = baseAlpha;
@@ -1274,7 +1274,7 @@ function drawCandleCrosshair(ctx, layout, palette, hoverX, candle, hoverTime, fo
   const time = formatTime(hoverTime);
   ctx.save();
   ctx.globalAlpha = opacity;
-  ctx.font = '400 13px "SF Mono", Menlo, monospace';
+  ctx.font = '400 13px "Libre Franklin", ui-sans-serif, sans-serif';
   ctx.textAlign = "left";
   let parts;
   if (layout.w >= 400) {
@@ -1353,7 +1353,7 @@ function drawLineModeCrosshair(ctx, layout, palette, hoverX, value, hoverTime, f
   const time = formatTime(hoverTime);
   ctx.save();
   ctx.globalAlpha = opacity;
-  ctx.font = '400 13px "SF Mono", Menlo, monospace';
+  ctx.font = '400 13px "Libre Franklin", ui-sans-serif, sans-serif';
   ctx.textAlign = "left";
   const parts = [
     { text: val, color: palette.line },
@@ -1399,7 +1399,7 @@ function drawEmpty(ctx, w, h, pad, palette, alpha = 1, now_ms = 0, skipLine = fa
   const text = emptyText ?? "No data to display";
   const amplitude = chartH * LOADING_AMPLITUDE_RATIO;
   ctx.save();
-  ctx.font = "400 12px system-ui, -apple-system, sans-serif";
+  ctx.font = '400 12px "Libre Franklin", ui-sans-serif, sans-serif';
   const textW = ctx.measureText(text).width;
   const gapHalf = textW / 2 + 20;
   const fadeW = 30;
@@ -1669,7 +1669,7 @@ function drawMultiFrame(ctx, layout, opts) {
         drawSimpleDot(ctx, lastPt[0], lastPt[1], entry.palette.line, 3);
       }
       if (entry.label) {
-        ctx.font = '600 10px -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif';
+        ctx.font = '600 10px "Libre Franklin", ui-sans-serif, sans-serif';
         ctx.textAlign = "left";
         ctx.fillStyle = entry.palette.line;
         ctx.fillText(entry.label, lastPt[0] + 6, lastPt[1] + 3.5);
@@ -3067,7 +3067,7 @@ function useLivelineEngine(canvasRef, containerRef, config) {
       const effectiveMultiSeries = useMultiStash ? lastMultiSeriesRef.current : cfg.multiSeries;
       let labelReserve = 0;
       if (effectiveMultiSeries.some((s) => s.label)) {
-        ctx.font = '600 10px -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif';
+        ctx.font = '600 10px "Libre Franklin", ui-sans-serif, sans-serif';
         let maxLabelW = 0;
         for (const s of effectiveMultiSeries) {
           if (s.label) {
@@ -3709,7 +3709,7 @@ function Liveline({
           display: "block",
           fontSize: 20,
           fontWeight: 500,
-          fontFamily: '"SF Mono", Menlo, monospace',
+          fontFamily: '"Libre Franklin", ui-sans-serif, sans-serif',
           color: isDark ? "rgba(255,255,255,0.85)" : "#111",
           transition: "color 0.3s",
           letterSpacing: "-0.01em",
@@ -3765,7 +3765,7 @@ function Liveline({
                     borderRadius: ws === "rounded" ? 999 : 4,
                     border: "none",
                     cursor: "pointer",
-                    fontFamily: "system-ui, -apple-system, sans-serif",
+                    fontFamily: '"Libre Franklin", ui-sans-serif, sans-serif',
                     fontWeight: isActive ? 600 : 400,
                     background: "transparent",
                     color: isActive ? activeColor : inactiveColor,
@@ -3929,7 +3929,7 @@ function Liveline({
               borderRadius: ws === "rounded" ? 999 : 4,
               border: "none",
               cursor: "pointer",
-              fontFamily: "system-ui, -apple-system, sans-serif",
+              fontFamily: '"Libre Franklin", ui-sans-serif, sans-serif',
               fontWeight: 500,
               background: isHidden ? "transparent" : ws === "text" ? "transparent" : isDark ? "rgba(255,255,255,0.06)" : "rgba(0,0,0,0.035)",
               color: isHidden ? inactiveColor : activeColor,
diff --git a/dist/index.js b/dist/index.js
index fd0506272e9a54432a0882c3a389ee74302e4718..436260e8327b3e629cd056874411b247f6ebef25 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -56,9 +56,9 @@ function resolveTheme(color, mode) {
     // Background
     bgRgb: isDark ? [10, 10, 10] : [255, 255, 255],
     // Fonts
-    labelFont: '11px "SF Mono", Menlo, Monaco, "Cascadia Code", monospace',
-    valueFont: '600 11px "SF Mono", Menlo, monospace',
-    badgeFont: '500 11px "SF Mono", Menlo, monospace'
+    labelFont: '11px "Libre Franklin", ui-sans-serif, sans-serif',
+    valueFont: '600 11px "Libre Franklin", ui-sans-serif, sans-serif',
+    badgeFont: '500 11px "Libre Franklin", ui-sans-serif, sans-serif'
   };
 }
 var SERIES_COLORS = [
@@ -612,7 +612,7 @@ function drawCrosshair(ctx, layout, palette, hoverX, hoverValue, hoverTime, form
   const separator = "  \xB7  ";
   ctx.save();
   ctx.globalAlpha = scrubOpacity;
-  ctx.font = '400 13px "SF Mono", Menlo, monospace';
+  ctx.font = '400 13px "Libre Franklin", ui-sans-serif, sans-serif';
   const valueW = ctx.measureText(valueText).width;
   const sepW = ctx.measureText(separator).width;
   const timeW = ctx.measureText(timeText).width;
@@ -664,7 +664,7 @@ function drawMultiCrosshair(ctx, layout, palette, hoverX, hoverTime, entries, fo
   if (scrubOpacity < 0.1 || layout.w < 300) return;
   ctx.save();
   ctx.globalAlpha = scrubOpacity;
-  ctx.font = '400 13px "SF Mono", Menlo, monospace';
+  ctx.font = '400 13px "Libre Franklin", ui-sans-serif, sans-serif';
   ctx.textAlign = "left";
   const timeText = formatTime(hoverTime);
   const sep = "  \xB7  ";
@@ -730,7 +730,7 @@ function drawReferenceLine(ctx, layout, palette, ref) {
   if (y < pad.top - 10 || y > h - pad.bottom + 10) return;
   const label = ref.label ?? "";
   if (label) {
-    ctx.font = "500 11px system-ui, sans-serif";
+    ctx.font = '500 11px "Libre Franklin", ui-sans-serif, sans-serif';
     const textW = ctx.measureText(label).width;
     const centerX = pad.left + chartW / 2;
     const gapPad = 8;
@@ -991,7 +991,7 @@ function drawOrderbook(ctx, layout, palette, orderbook, dt, state, swingMagnitud
   state.labels.length = writeIdx;
   const baseAlpha = ctx.globalAlpha;
   ctx.save();
-  ctx.font = '600 13px "SF Mono", Menlo, monospace';
+  ctx.font = '600 13px "Libre Franklin", ui-sans-serif, sans-serif';
   ctx.textAlign = "left";
   ctx.textBaseline = "middle";
   ctx.globalAlpha = baseAlpha;
@@ -1247,7 +1247,7 @@ function drawCandleCrosshair(ctx, layout, palette, hoverX, candle, hoverTime, fo
   const time = formatTime(hoverTime);
   ctx.save();
   ctx.globalAlpha = opacity;
-  ctx.font = '400 13px "SF Mono", Menlo, monospace';
+  ctx.font = '400 13px "Libre Franklin", ui-sans-serif, sans-serif';
   ctx.textAlign = "left";
   let parts;
   if (layout.w >= 400) {
@@ -1326,7 +1326,7 @@ function drawLineModeCrosshair(ctx, layout, palette, hoverX, value, hoverTime, f
   const time = formatTime(hoverTime);
   ctx.save();
   ctx.globalAlpha = opacity;
-  ctx.font = '400 13px "SF Mono", Menlo, monospace';
+  ctx.font = '400 13px "Libre Franklin", ui-sans-serif, sans-serif';
   ctx.textAlign = "left";
   const parts = [
     { text: val, color: palette.line },
@@ -1372,7 +1372,7 @@ function drawEmpty(ctx, w, h, pad, palette, alpha = 1, now_ms = 0, skipLine = fa
   const text = emptyText ?? "No data to display";
   const amplitude = chartH * LOADING_AMPLITUDE_RATIO;
   ctx.save();
-  ctx.font = "400 12px system-ui, -apple-system, sans-serif";
+  ctx.font = '400 12px "Libre Franklin", ui-sans-serif, sans-serif';
   const textW = ctx.measureText(text).width;
   const gapHalf = textW / 2 + 20;
   const fadeW = 30;
@@ -1642,7 +1642,7 @@ function drawMultiFrame(ctx, layout, opts) {
         drawSimpleDot(ctx, lastPt[0], lastPt[1], entry.palette.line, 3);
       }
       if (entry.label) {
-        ctx.font = '600 10px -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif';
+        ctx.font = '600 10px "Libre Franklin", ui-sans-serif, sans-serif';
         ctx.textAlign = "left";
         ctx.fillStyle = entry.palette.line;
         ctx.fillText(entry.label, lastPt[0] + 6, lastPt[1] + 3.5);
@@ -3040,7 +3040,7 @@ function useLivelineEngine(canvasRef, containerRef, config) {
       const effectiveMultiSeries = useMultiStash ? lastMultiSeriesRef.current : cfg.multiSeries;
       let labelReserve = 0;
       if (effectiveMultiSeries.some((s) => s.label)) {
-        ctx.font = '600 10px -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif';
+        ctx.font = '600 10px "Libre Franklin", ui-sans-serif, sans-serif';
         let maxLabelW = 0;
         for (const s of effectiveMultiSeries) {
           if (s.label) {
@@ -3682,7 +3682,7 @@ function Liveline({
           display: "block",
           fontSize: 20,
           fontWeight: 500,
-          fontFamily: '"SF Mono", Menlo, monospace',
+          fontFamily: '"Libre Franklin", ui-sans-serif, sans-serif',
           color: isDark ? "rgba(255,255,255,0.85)" : "#111",
           transition: "color 0.3s",
           letterSpacing: "-0.01em",
@@ -3738,7 +3738,7 @@ function Liveline({
                     borderRadius: ws === "rounded" ? 999 : 4,
                     border: "none",
                     cursor: "pointer",
-                    fontFamily: "system-ui, -apple-system, sans-serif",
+                    fontFamily: '"Libre Franklin", ui-sans-serif, sans-serif',
                     fontWeight: isActive ? 600 : 400,
                     background: "transparent",
                     color: isActive ? activeColor : inactiveColor,
@@ -3902,7 +3902,7 @@ function Liveline({
               borderRadius: ws === "rounded" ? 999 : 4,
               border: "none",
               cursor: "pointer",
-              fontFamily: "system-ui, -apple-system, sans-serif",
+              fontFamily: '"Libre Franklin", ui-sans-serif, sans-serif',
               fontWeight: 500,
               background: isHidden ? "transparent" : ws === "text" ? "transparent" : isDark ? "rgba(255,255,255,0.06)" : "rgba(0,0,0,0.035)",
               color: isHidden ? inactiveColor : activeColor,
