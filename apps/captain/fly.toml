app = "opemipo-flight-agent"
primary_region = "lhr"

[build]
  dockerfile = "Dockerfile"

[deploy]
  release_command = "/bin/sh -c 'node --import tsx scripts/migrate.ts && ./node_modules/.bin/bootstrap'"

[env]
  AI_MODEL = "openai/gpt-oss-20b"
  PORT = "8080"
  CAPTAIN_PUBLIC_URL = "https://opemipo-flight-agent.fly.dev"
  PILOT_BASE_URL = "https://opemipo-captain.fly.dev"
  CAPTAIN_BASIC_USERNAME = "captain"
  CAPTAIN_OWNER_AUTH_ENABLED = "true"
  DUFFEL_BASE_URL = "https://api.duffel.com"
  DUFFEL_LIVE_MODE = "false"
  DUFFEL_SUPPLIER_TIMEOUT_MS = "20000"
  FLIGHT_SEARCH_TIMEOUT_MS = "120000"
  WORKFLOW_POSTGRES_JOB_PREFIX = "captain_eve"
  GRAPHILE_WORKER_SCHEMA = "captain_worker"
  WORKFLOW_POSTGRES_WORKER_CONCURRENCY = "4"
  WORKFLOW_POSTGRES_MAX_POOL_SIZE = "8"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 1

  [[http_service.checks]]
    grace_period = "12s"
    interval = "30s"
    method = "GET"
    path = "/ready"
    timeout = "6s"

[[vm]]
  size = "shared-cpu-1x"
  memory = "1gb"
  cpus = 1
